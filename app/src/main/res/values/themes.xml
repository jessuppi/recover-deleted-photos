<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base Material 3 theme (Day/Night) with no action bar.
         Our toolbar lives in activity_main.xml and is owned by MainActivity. -->
    <style name="Theme.RecoverDeletedPhotos" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Brand & surface colors -->
        <item name="colorPrimary">@color/md_seed</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_on_surface</item>
        <item name="colorOutline">@color/md_outline</item>

        <!-- Default toolbar style for MaterialToolbar -->
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar.Surface</item>

        <!-- NOTE:
             We intentionally omit android:statusBarColor, android:navigationBarColor,
             android:windowLightStatusBar, android:windowLightNavigationBar and all tools:
             flags. Material3 + DayNight will handle contrast & system bars automatically. -->
    </style>

</resources>
